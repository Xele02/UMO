using GooglePlayGames;

namespace GooglePlayGames.BasicApi
{
	public class Player : PlayGamesUserProfile
	{
		internal Player(string displayName, string playerId, string avatarUrl) : base(default(string), default(string), default(string))
		{
		}

	}
}
